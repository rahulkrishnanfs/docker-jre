machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build  rahulkrishnanfs/docker-jre

test:
  override:
    - docker run  rahulkrishnanfs/docker-jre ; sleep 10
